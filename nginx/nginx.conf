events {
    worker_connections 1024;
    }

http {
    include       mime.types;
    default_type application/octet-stream;

    sendfile        on;
    server_tokens off;

    upstream backend {
        server app:8080;
    }

    server {
        listen 80;
        server_name api.vpnrocket.shop vpnrocket.shop;

        location /.well-known/acme-challenge/ {
            root /var/www/certbot;
            allow all;
        }

        location / {
            return 301 https://$host$request_uri;
        }
    }

    server {
        listen 443 ssl;
        server_name api.vpnrocket.shop;

        ssl_certificate /etc/letsencrypt/live/api.vpnrocket.shop/fullchain.pem;
        ssl_certificate_key /etc/letsencrypt/live/api.vpnrocket.shop/privkey.pem;

        location / {
            proxy_pass http://backend;

            proxy_http_version 1.1;
            proxy_set_header Upgrade $http_upgrade;
            proxy_set_header Connection "upgrade";
            proxy_set_header Host $host;
            proxy_set_header X-Real-IP $remote_addr;
            proxy_set_header X-Forwarded-For $proxy_add_x_forwarded_for;
            proxy_set_header X-Forwarded-Proto $scheme;
        }
    }

    server {
        listen 443 ssl;
        server_name vpnrocket.shop;

        ssl_certificate /etc/letsencrypt/live/vpnrocket.shop/fullchain.pem;
        ssl_certificate_key /etc/letsencrypt/live/vpnrocket.shop/privkey.pem;

        root /usr/share/nginx/html;
        index index.html;

        location ~* \.(?:css|js|jpg|jpeg|gif|png|ico|svg|woff2?|ttf)$ {
            try_files $uri =404;
            expires 30d;
            add_header Cache-Control "public, immutable";
        }

        location / {
            try_files $uri $uri/ /index.html;
        }

    }
}
